# Dashboard emissions programme — completed record

This directory preserves the 6–7 August measure-aware dashboard plan, its
overnight execution programme, and the completed relay log. The relay reached
`COMPLETE`; subsequent dashboard work has continued on `master` and the files
here are not active prompts.

Durable behavior is documented in the dashboard guide, mapping-consumer guide,
special-rules log, and work queue. Re-scope any unfinished idea against current
code and the manifested parquet input rather than restarting these prompts.
