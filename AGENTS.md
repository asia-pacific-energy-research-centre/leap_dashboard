# AGENTS.md

These are project-level instructions for Codex and similar agents.

## Repo Scope

- This repo owns LEAP dashboard implementation and dashboard template edits.
- For dashboard navigation/template work, use `config/leap_comparison_dashboard_template_v3.json` unless the user names a different template.
- Do not make LEAP dashboard changes in `C:\Users\Work\github\leap_utilities` unless the user explicitly asks for shared utility code changes there.
